fastapi
uvicorn[standard]
sqlalchemy
aiomysql
python-dotenv
prometheus-fastapi-instrumentator
python-json-logger
# The shared models are installed via the Dockerfile COPY command,
# but listing it here helps local development environments.
# This path is relative to the service directory.
-e ../../shared/models